
    const sum = (a,b) => a+b;
    const mul = (a,b) => a*b;
    const g = 9.8
    const pi = 3.14


    let obj ={
        sum:sum(2,3),
        Mul:mul,
        g:g,
        pi:pi,
    };

    module.exports =obj;



    // export const sum = (a,b) => a+b;
    // export const mul = (a,b) => a*b;
    // export const g = 9.8
    // export const pi = 3.14